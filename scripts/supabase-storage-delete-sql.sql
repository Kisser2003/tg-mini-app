-- =============================================================================
-- Альтернатива JS-скрипту: очистка метаданных объектов Storage через SQL
-- =============================================================================
-- Запуск в SQL Editor под ролью с правами на storage (часто service_role / postgres).
-- После DELETE объекты пропадут из Storage API; предпочтительно всё же
-- scripts/empty-storage-buckets.mjs — он использует официальный API.
--
-- DELETE FROM storage.objects;
--
-- Если в проекте есть кастомные политики RLS на storage.objects, может потребоваться
-- выполнение от имени суперпользователя или через Dashboard → Storage → очистка вручную.
